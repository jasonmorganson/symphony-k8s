apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: symphony-orchestrator
  namespace: symphony
spec:
  rules:
    - http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: symphony-orchestrator
                port:
                  number: 4000
