apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: symphony-worker
  namespace: symphony
  labels:
    app: symphony-worker
spec:
  serviceName: symphony-worker
  podManagementPolicy: Parallel
  replicas: 3
  updateStrategy:
    type: OnDelete
  selector:
    matchLabels:
      app: symphony-worker
  template:
    metadata:
      labels:
        app: symphony-worker
    spec:
      automountServiceAccountToken: false
      securityContext:
        seccompProfile:
          type: RuntimeDefault
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            - labelSelector:
                matchLabels:
                  app: symphony-worker
              topologyKey: kubernetes.io/hostname
          preferredDuringSchedulingIgnoredDuringExecution:
            - weight: 75
              podAffinityTerm:
                labelSelector:
                  matchLabels:
                    app: symphony-orchestrator
                topologyKey: kubernetes.io/hostname
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app: symphony-worker
      initContainers:
        - name: init-workspace-permissions
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              mkdir -p /srv/symphony/workspaces
              mkdir -p /srv/worker-data/codex-home
              mkdir -p /srv/worker-data/local-home/share/mise
              mkdir -p /srv/worker-data/local-home/state/mise
              if [ ! -s /srv/worker-data/codex-home/auth.json ]; then
                cp /codex-auth/auth.json /srv/worker-data/codex-home/auth.json.next
                chown 10001:10001 /srv/worker-data/codex-home/auth.json.next
                chmod 0600 /srv/worker-data/codex-home/auth.json.next
                mv /srv/worker-data/codex-home/auth.json.next \
                  /srv/worker-data/codex-home/auth.json
              fi
              chown -R 10001:10001 /srv/worker-data/codex-home
              chown -R 10001:10001 /srv/worker-data/local-home
              chmod 0700 /srv/worker-data/codex-home
              chmod 0755 /srv/worker-data/local-home
              chmod 0600 /srv/worker-data/codex-home/auth.json
              chmod 0777 /srv/symphony/workspaces
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: workspaces
              mountPath: /srv/symphony/workspaces
            - name: workspaces
              mountPath: /srv/worker-data
            - name: codex-chatgpt-auth
              mountPath: /codex-auth
              readOnly: true
        - name: init-worker-hostkeys
          image: busybox:1.36
          command:
            - sh
            - -c
            - |
              cp /ssh-src/* /ssh-dst/
              chmod 600 /ssh-dst/ssh_host_ed25519_key
              chmod 644 /ssh-dst/ssh_host_ed25519_key.pub
              chmod 755 /ssh-dst
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: worker-hostkeys-secret
              mountPath: /ssh-src
              readOnly: true
            - name: worker-hostkeys
              mountPath: /ssh-dst
      containers:
        - name: worker
          image: symphony-worker
          imagePullPolicy: IfNotPresent
          ports:
            - name: ssh
              containerPort: 22
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - test -f /run/symphony-worker-ready
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            tcpSocket:
              port: ssh
            periodSeconds: 30
            failureThreshold: 3
          env:
            - name: LINEAR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: symphony-secrets
                  key: LINEAR_API_KEY
            - name: GITHUB_TOKEN
              valueFrom:
                secretKeyRef:
                  name: github-machine-arrusted-symphony
                  key: token
                  optional: false
          securityContext:
            allowPrivilegeEscalation: false
          volumeMounts:
            - name: workspaces
              mountPath: /srv/symphony/workspaces
            - name: workspaces
              mountPath: /home/symphony/.codex
              subPath: codex-home
            - name: codex-chatgpt-auth
              mountPath: /etc/symphony/codex-auth
              readOnly: true
            - name: workspaces
              mountPath: /home/symphony/.local
              subPath: local-home
            - name: worker-hostkeys
              mountPath: /etc/ssh/keys
              readOnly: true
            - name: worker-authorized-keys
              mountPath: /etc/ssh/authorized-keys/authorized_keys
              subPath: authorized_keys
              readOnly: true
        - name: workspace-reclaimer
          image: symphony-worker
          imagePullPolicy: IfNotPresent
          command:
            - /usr/local/bin/workspace-reclaimer
          ports:
            - name: reclaimer-http
              containerPort: 8081
          livenessProbe:
            httpGet:
              path: /healthz
              port: reclaimer-http
            periodSeconds: 60
          env:
            - name: LINEAR_API_KEY
              valueFrom:
                secretKeyRef:
                  name: symphony-secrets
                  key: LINEAR_API_KEY
            - name: SYMPHONY_STATE_URL
              value: http://symphony-orchestrator:4000/api/v1/state
            - name: WORKSPACE_RECLAIMER_INTERVAL_SECONDS
              value: "600"
            - name: WORKSPACE_RECLAIMER_GRACE_SECONDS
              value: "3600"
            - name: WORKSPACE_RECLAIMER_CONFIRMATIONS
              value: "2"
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            runAsNonRoot: true
            runAsUser: 10001
            runAsGroup: 10001
            capabilities:
              drop: ["ALL"]
          volumeMounts:
            - name: workspaces
              mountPath: /srv/symphony/workspaces
            - name: workspaces
              mountPath: /srv/worker-data
      volumes:
        - name: codex-chatgpt-auth
          secret:
            secretName: codex-chatgpt-auth
            defaultMode: 0400
            items:
              - key: auth.json
                path: auth.json
        - name: worker-hostkeys-secret
          secret:
            secretName: symphony-worker-hostkeys
            defaultMode: 0600
            items:
              - key: ssh_host_ed25519_key
                path: ssh_host_ed25519_key
              - key: ssh_host_ed25519_key.pub
                path: ssh_host_ed25519_key.pub
        - name: worker-hostkeys
          emptyDir: {}
        - name: worker-authorized-keys
          secret:
            secretName: symphony-worker-authorized-keys
            defaultMode: 0400
            items:
              - key: authorized_keys
                path: authorized_keys
  volumeClaimTemplates:
    - metadata:
        name: workspaces
      spec:
        accessModes: ["ReadWriteOnce"]
        resources:
          requests:
            storage: 20Gi
