apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: symphony-orchestrator-workspaces
  namespace: symphony
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 20Gi
